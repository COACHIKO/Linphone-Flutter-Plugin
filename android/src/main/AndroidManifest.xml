<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <application
        android:usesCleartextTraffic="true">

        <service
            android:name="org.linphone.core.tools.service.CoreService"
            android:foregroundServiceType="phoneCall|microphone"
            android:enabled="false"
            android:stopWithTask="false" />

        <!-- BroadcastReceiver for handling incoming call notification actions -->
        <receiver
            android:name=".CallActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.egytelecoms.hatif.ACTION_ANSWER_CALL" />
                <action android:name="com.egytelecoms.hatif.ACTION_DECLINE_CALL" />
            </intent-filter>
        </receiver>
        
        <!-- BroadcastReceiver for launching IncomingCallActivity from terminated state -->
        <receiver
            android:name=".IncomingCallReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.egytelecoms.hatif.ACTION_INCOMING_CALL" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
